import React from 'react'

const Button = ({ title, id, rightIcon, leftIcon, containerClass}) => {
  return (
    <button id={id} className="group relative z-10 w-fit cursor-pointer overflow-hidden rounded-full">

    </button>
  )
}

export default Button